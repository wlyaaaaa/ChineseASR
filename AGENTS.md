# ChineseASR 项目规则

本文件约束项目业务语义；本机任务、运行时和恢复事实归 PCConfig，Git 事实归 GitHub 总索引。

- 文件转写新增 `high_quality` 命名配置（FireRedASR2-LLM + Qwen3-ASR），`baseline` 是可回退对照；用配置显式选择，不把模型一致、对齐成功或文件完整当作文字正确。短/长/批量入口必须遵守模型时长约束，保留原音频与声道来源。质量旁路结果与原始审计证据分开，不因补充功能失败删除正文。维护流程见 `docs/quality-and-model-maintenance.md`。
- 录音转写默认维持 `configs/models.yaml`：quick 用 SenseVoiceSmall，strict 用 Qwen3-ASR-1.7B + SenseVoiceSmall；保留现有重要录音、时间戳和说话人专用路线。
- 桌面听写配置在 `configs/dictation.yaml`，当前使用单个 Qwen3-ASR-1.7B。Win+H 与 Ctrl+Win+H 均显示/隐藏小窗并继续/暂停录音，含远程按键；按配置的显示器硬件型号 ID 优先级只显示一个小窗：VDD 接入时优先 VDD，断开后回退主屏；随屏幕接入/断开和分辨率变化重新定位，保留左侧自由拖动，手动位置不因状态刷新或显示/隐藏被复位。录音按钮单独切换录音/暂停，×隐藏并暂停，主界面极简白底绿图标，小窗为 200×75 实际像素（原尺寸放大 25%），状态说明使用 20 像素文字：录音按钮、小箭头展开设备菜单及小型×；麦克风图标右上角设可点击的状态点，绿色表示就绪、黄色表示准备或等待、红色表示故障，点开显示当前状态、原因和处理提示；保留悬停提示，不增加常驻标题、说明或设备栏。中文及中英技术词、停止后的速度是主要体验；不自动发送文字，不增加 LLM 润色或双模型等待。
- 用户已允许 AI 自主做兼容、可回退的依赖更新。有新版本不等于有更好的模型：没有明确改进证据就维持现有模型；出现可靠且明显更好的中文模型时，可以由 AI 判断新增或替换。
- 模型判断优先查同口径公开中文评测、官方说明及可靠独立复现，同时考虑中文实际效果、中英混说、速度与本机兼容。单项跑分、版本更新日期、参数更大或厂商宣传不能独自证明值得替换。不同用途分别判断，不因听写更新改掉重要录音的能力。
- 将换模依据、适用场景、兼容结论和回退方式简要记录在本项目现有配置说明/提交中。无需为了选型下载多个大模型交叉比赛；公共短音频可查集成和延迟，个人口音与麦克风体验由用户试用。私人录音检索需明确任务，不把媒体库当默认测试集。
- 上述维护授权不自带更新周期；不据此新建定时模型下载或后台 AI 更新服务。依赖 pin（固定版本）用于复现和兼容，可在验证后更新；Qwen 的权重身份和运行时约束一起维护。
- 听写复用项目适配器，绕开文件转写的作业/审计链；GPU 工作通过 LocalGpuBroker。用户选定的资源策略是 OCR 与 ASR 可并行，Ollama 大语言模型与二者互斥；同类任务仍串行，不绕过 Broker。空闲将听写模型放回内存并释放租约。录音和最近文本只放内存，用户主动复制时才写剪贴板。
- 听写模型加载与推理须有有界恢复，区分加载、真实 GPU 排队与麦克风异常，不用永久“准备中”掩盖失败。ASR/OCR 不得主动暂停或停止 Wallpaper Engine，不得修改无关的全屏、最大化或其他程序播放规则；后台助手保持隐藏。
- 入口：`.venv\Scripts\python.exe -m zh_asr.dictation`；登录自启由 `scripts\dictation.ps1 -Mode Install` 管理。常规验证：`python -m unittest discover -s tests`；不得用软件检查冒充用户实际语音准确率。
- 源音频、识别正文、模型权重、运行日志及私人评测材料不进入公开 Git。保留原始录音和已有并发工作。
