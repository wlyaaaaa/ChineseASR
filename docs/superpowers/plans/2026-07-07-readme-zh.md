# README Chinese Localization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite `README.md` into a comprehensive Simplified Chinese entry document for ChineseASR.

**Architecture:** This is a documentation-only change. The README becomes the public first-use guide, while `docs/architecture.md` remains the deeper architecture reference and `docs/public-release.md` remains the publication boundary reference.

**Tech Stack:** Markdown, PowerShell command examples, existing ChineseASR repository docs.

## Global Constraints

- Keep commands, model names, API endpoints, config keys, filenames, and JSON field names in English.
- Do not include private audio, generated transcript content, tokens, raw logs, OAuth JSON, absolute private output examples, or model weights.
- Do not change runtime code, scripts, tests, or `configs\models.yaml`.
- Keep the public repository boundary explicit: `models/`, `outputs/`, `eval/corpus/`, and `offline/wheelhouse/` stay ignored.

---

### Task 1: Rewrite README

**Files:**
- Modify: `README.md`
- Create: `docs/superpowers/specs/2026-07-07-readme-zh-design.md`
- Create: `docs/superpowers/plans/2026-07-07-readme-zh.md`

**Interfaces:**
- Consumes: existing `README.md`, `docs/architecture.md`, `docs/public-release.md`.
- Produces: Chinese README with setup, usage, API, evaluation, offline, model replacement, testing, and publication boundary sections.

- [x] **Step 1: Read current public docs**

Run: `Get-Content README.md`, `Get-Content docs\architecture.md`, `Get-Content docs\public-release.md`

Expected: current README is English; architecture and public-release docs contain the source facts for the Chinese rewrite.

- [x] **Step 2: Replace README content**

Write a Chinese README with these exact top-level sections:

```markdown
# ChineseASR
## 当前状态
## 适合场景
## 不适合场景
## 默认模型策略
## 一分钟使用
## 安装与模型下载
## 输出文件
## 本地 API 与 Smart Wrapper
## 长音频与断点续跑
## LLM 仲裁
## 评测与 Benchmark
## 离线 Wheelhouse
## 模型替换
## 测试
## 公开仓库边界
## 常见问题
## 更多文档
```

Expected: README is readable in Simplified Chinese and keeps technical identifiers in English.

- [x] **Step 3: Verify docs and repository**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
.\scripts\doctor.ps1
git diff --check
```

Then run the repository's public-safe source scan over `README.md`, `docs`, `src`, `tests`, `scripts`, and `configs`.

Expected: unit tests pass, doctor succeeds, diff check has no whitespace errors, and the public-safe scan has no matches.

- [x] **Step 4: Close out Git**

Run:

```powershell
git add -- README.md docs/superpowers/specs/2026-07-07-readme-zh-design.md docs/superpowers/plans/2026-07-07-readme-zh.md
git commit -m "docs: localize ChineseASR README"
git push
```

Expected: `wlyaaaaa/ChineseASR` `main` is pushed, then `E:\GitHub总索引` records the push.
